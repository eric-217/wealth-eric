# 客户财富管理规划报表 · 文件架构

## 部署说明

将本文件夹上传至 **Cloudflare Pages** 即可运行。

```
上传目录: D:\workbuddy\财富管理\客户财富管理规划报表\
```

## 文件夹结构

```
客户财富管理规划报表/
│
├── index.html              ← 首页：「我的财富管理规划」+ 登录入口
├── login.html              ← 登录页：手机号 + 密码
├── dashboard.html          ← 报表页：通过认证后加载客户数据
│
├── assets/
│   ├── auth.js             ← 认证系统（客户账号 + 页面登录守卫 initAuth()）
│   └── style.css           ← 全局样式
│
├── tools/
│   └── inject_auth.py      ← 批量注入页面登录守卫（新页面运行一次）
│
├── products/               ← 产品资料（所有客户共享）
│   ├── index.html          ← 产品目录
│   └── template.html       ← 产品一页通模板
│
├── clients/                ← 客户数据目录（每个客户一个子目录）
│   ├── chen/               ← 陈女士（正式版，真实数据）
│   │   ├── 菜单.html
│   │   ├── 资产明细.html    → H5手风琴折叠单页
│   │   ├── 现金流.html      → H5手风琴折叠单页 + 保险流嵌套面板
│   │   └── ...（共14个页面）
│   └── template/           ← 脱敏模板（数据×0.6，新增客户时复制）
│       └── ...（与chen目录结构一致）
│
└── README.md               ← 本文件
```

## 用户访问流程

```
打开网站 → 首页（index.html）
  → 点击「登录」
  → 登录页（login.html）输入手机号+密码
  → 验证通过 → 进入 dashboard.html
  → 页面加载该客户的数据文件（iframe嵌入）
```

## 新增客户步骤

1. 将新客户的财务报告 HTML 文件放入 `clients/[客户目录]/`
2. 在 `assets/auth.js` 的 `CLIENTS` 对象中添加一条记录：

```js
'13900000000': {
  name: '新客户名称',
  path: '新客户目录/财务报告.html',
  hash: '<SHA-256哈希>'
}
```

3. 哈希生成方式：浏览器控制台运行
```js
crypto.subtle.digest('SHA-256', new TextEncoder().encode('密码'))
  .then(h => Array.from(new Uint8Array(h)).map(b => b.toString(16).padStart(2,'0')).join(''))
```

4. 上传到 Cloudflare Pages，完成。

## 当前已配置客户

| 手机号 | 客户 | 状态 |
|------|------|------|
| 13800000000 | 谭女士家庭 | ✅ 已配置 |

## 技术说明

- **纯静态**：HTML + CSS + JS，无需后端服务器
- **认证**：Web Crypto API SHA-256（浏览器原生支持的加密标准）
- **页面守卫**：每个客户页面内置 `initAuth()`，未登录自动跳转。一次登录全局有效
- **密码不可逆**：密码以哈希存储，源码看不到明文
- **部署**：上传到 Cloudflare Pages 即可
- **H5设计**：资产明细/现金流采用手风琴折叠面板，默认全部收起，手机端友好

## 当前已配置客户

| 手机号 | 客户 | 状态 |
|------|------|------|
| 15107572598 | 陈女士 | ✅ 已配置 |
| 13800000000 | 模板客户 | ✅ 已配置 |

## 跨对话任务追踪

所有待办、进行中、已完成事项统一记录在 [ISSUES.md](ISSUES.md)，覆盖四大工作主题：

| 主题 | 范围 |
|------|------|
| 财富管理 | 陈女士/孙小姐等客户资产规划 |
| 固收分析 | 固收产品风险监控、公众号文章 |
| 境外资产 | 港险、CRS、境外配置 |
| 教育理论 | 教育方法讨论、资料整理 |

**规则**：每次对话开始 AI 自动读取，完成后自动更新。手机上通过 GitHub App 可随时查看。
