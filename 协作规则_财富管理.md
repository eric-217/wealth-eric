# 协作规则 — 财富管理

> 所有客户共用。本文件存放财富管理主题的分析方法、工作约定、模板规范。

## 一、客户网站建设

### 页面模板同步
- 修改 `template/` 目录下任何页面时，必须同步更新所有客户对应页面
- 配色例外：各客户可保留独立配色方案（陈女士蓝、孙小姐紫）
- 同步方式：修改 template → diff 对比 → 合并到各客户页面

### 配色约定
| 客户 | 目录 | 主题色 | 主色调 |
|------|------|--------|--------|
| 模板 | template/ | 蓝 | #1a56db |
| 陈女士 | chen/ | 蓝 | #1a56db |
| 孙小姐 | sun/ | 紫 | #6B5B9A |

### 新增客户
1. 复制 `template/` 目录 → `clients/[新客户]/`
2. 在 `assets/auth.js` 中添加认证记录
3. 修改配色方案
4. 填充真实数据

## 二、数据联动

- 总览页数据从资产明细、负债明细、现金流等页面汇总
- 各页面之间数据必须一致（同一笔资产在不同页面的金额、名称必须匹配）
- 修改数据时先改明细页，再同步总览页

## 三、合规与隐私

### 网页输出脱敏规则
- 客户名称在前端页面使用简称（如"孙小姐"、"陈女士"）
- 禁止在前端 HTML 中出现完整姓名、身份证号、详细地址
- 保单号、银行账号可保留后4位
- 文件/文件夹命名可使用全名（仓库私有，安全性可控）

### 数据安全
- 客户数据推送前必须确认：是否包含新敏感信息
- 涉及客户数据的内容推送前先询问用户

## 四、Git 与部署

### 推送规则
- 每完成实质工作后：`git add -A && git commit -m "描述" && git push`
- 推送前判断内容类型：
  - 客户可见（报表、配置建议等）→ 适合推送
  - 内部协作（清单、规则文档、客户敏感数据）→ 询问用户
- 阶段性改动完成后主动提醒"可以推上 GitHub 让 Cloudflare 自动部署"

### Releases 版本管理
- 标签格式：`vYYYY.MM`（如 v2026.07）
- 触发条件：季度结束 或 重大更新完成 或 用户说"打标签"
- 命令：`git tag -a vYYYY.MM -m "描述" && git push origin vYYYY.MM`

## 五、数据分析方法

### 资产配置分析框架
- 按流动性分层：活期 → 固收 → 权益 → 另类
- 按风险分层：低风险 → 中低风险 → 中高风险
- 按期限分层：短期(<1年) → 中期(1-3年) → 长期(>3年)

### 现金流分析
- 固收产品到期台账：产品名称、到期日、金额、续投方案
- 保险缴费节奏：年缴保费、缴费时间、保障期限

### 待补充的客户专属框架
- 孙瑞丽专属分析框架：（待根据实际数据和分析需求补充到本节）

## 六、ISSUES 文件管理

- 每个客户独立的 ISSUES 文件：`ISSUES_财富管理-[客户全名].md`
- 本对话（孙瑞丽）：`ISSUES_财富管理-孙瑞丽.md`
- 陈女士对话：`ISSUES_财富管理-陈莹.md`
- 手机端可直接编辑 GitHub 上的对应文件

---

*最后更新：2026-07-25*
